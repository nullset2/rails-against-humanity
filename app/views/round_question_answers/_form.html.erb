<%= form_for(round_question_answer) do |f| %>
  <% if round_question_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(round_question_answer.errors.count, "error") %> prohibited this round_question_answer from being saved:</h2>

      <ul>
      <% round_question_answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :round_question_id %>
    <%= f.number_field :round_question_id %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
