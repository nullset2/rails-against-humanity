<%= form_for(game_entry) do |f| %>
  <% if game_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(game_entry.errors.count, "error") %> prohibited this game_entry from being saved:</h2>

      <ul>
      <% game_entry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :game_id %>
    <%= f.number_field :game_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
